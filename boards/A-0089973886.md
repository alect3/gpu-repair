# Board A

- Model: Zotac RTX 3090 Trinity
- Serial: 0089973886
- Status: dead, no power at all

## Symptoms

- No power at all when booted. No fans, no lights, no POST.
- The PC itself ran fine with the card installed. The PSU did not trip, so
  the card is not shorting under power. It is simply not enabling.

## Findings

- Resistance to ground is normal on every rail: 12V, 5V, 3.3V, 1.8V, PEX,
  core and memory
  (measured 2026-09-05).
- Two capacitors and a resistor had fallen off (same as Board B, see
  [common.md](../common.md)). Re-soldered before testing began. 5V reads
  normal to ground, so the re-fit on this board did not introduce a short.
- 12V input path from the 8-pin connectors to the VRM input is intact
  (continuity, 2026-09-05).
- Suspected to be a different fault from Board B. The missing parts alone
  would not explain a total no-power condition, so something else is wrong.

## Log

### 2026-09-04

- Tried to boot. No power at all.

### 2026-09-05

- Two capacitors and a resistor had fallen off (same as Board B). Re-soldered
  them before testing.
- 12V rail resistance to ground is fine. No short on 12V input.
- 1.8V rail resistance to ground is fine.
- PEX rail resistance to ground is fine.
- Core and memory rail resistance to ground seem normal.
- 5V rail resistance to ground is fine.
- 3.3V rail resistance to ground is fine.
- Looked for input fuses. None found.
- Continuity from 8-pin 12V pins to VRM input is fine.
- Slot 12V fingers to the core VRM high-side FET drains: no continuity
  beep, about 50 kohm in resistance mode. Not yet known whether this is a
  fault or by design (see next steps).
- First powered test. 12V present on both 8-pins. 0V on 5V, 1.8V, PEX,
  core and memory. 3.3V not measured. Card was on a riser fed from the
  ATX PSU, riser light on, no PC. Slot 12V and 3.3V delivery not yet
  verified at the riser.
- Confirmed 3.3V at the riser's regulator output. Slot power is being
  delivered. The dead small rails are a real fault on the card.
- Small regulator inductors, powered on riser, viewed from the front:
  - Top left inductor: 0V. Identity not yet confirmed (5V or PEX).
  - Bottom left inductor, assumed 1.8V: about 300 mV. Not a dead rail,
    something is either leaking into it or the regulator is trying and
    failing to start.

## Next steps

No rail is shorted, the 12V input path is intact and matches Board B, and
the PC ran fine with the card in. So it is not a short under power. The
card is not enabling: either 12V is not arriving, or the sequencing chain
is not starting the regulators.

Bench setup: card on a powered PCIe riser (riser fed from the spare ATX
PSU, no PC needed for this stage). The riser supplies slot 12V and 3.3V.
Same ATX PSU on the 8-pins, jumpered on with PS_ON to ground. The slot
has no power-good line, only PERST# (reset), and the small rails should
come up without it. A 12V headlight bulb in series with the 8-pin 12V is a cheap
current limiter: dim is normal, bright is a short.

Powered. 12V on the 8-pins confirmed. Every small rail is dead, so the
sequence is not starting at all. Next, in order:

1. Riser 3.3V confirmed. Optionally confirm it on the card side at a
   capacitor just behind the edge connector.
2. 12V at the VRM input bulk capacitors for core and memory.
3. Identify the small regulators. For each small inductor, read the marking
   on the IC beside it, then measure its input pin (or input cap), enable
   pin, and output (the inductor). Two found so far: top left (0V) and
   bottom left (about 300 mV, assumed 1.8V). Look for others, including on
   the back.
   - No input: the 12V feed to it is broken.
   - Input but no output and enable low: something upstream is holding it
     off, or the enable resistor is one of the re-fitted parts.
   - Input, enable high, no output: dead regulator.
4. Compare every reading in step 3 with the same points on Board B once its
   5V short is fixed. Same board, so the numbers should match.
5. Only once 5V and 3.3V are up does the rest of the chain (1.8V, PEX,
   on-board power-good, core, memory) mean anything.
